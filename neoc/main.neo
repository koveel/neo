printf :: (msg: string);

Player :: struct
{
    name: string;
}

main :: () -> i32
{
    p := Player {
        "sonic the hedgehog\n"
    };

    printf(p.name);
    printf("Hello, World!");
    return 0;
}