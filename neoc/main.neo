printf :: (msg: string);

Food :: enum
{
    Apple,
    Banana,
    Bread,
    Cake,
}

print :: (msg: string)
{
    printf(msg);
    printf("\n");
}

main :: () -> i32
{
    nums := [] {
        0, 1, 0, 3, 4, 5
    };
    
    second := @nums[0];
    return *(second + nums[2]);
}