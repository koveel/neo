printf :: (msg: string);

Food :: enum
{
    Apple,
    Banana,
    Bread,
    Cake,
}

main :: () -> i32
{
    msgs := [] {
        "One",
        "Two",
    };

    for msg: msgs {
        printf(msg);
        printf("\n");
    }

    return 2;
}