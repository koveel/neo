printf :: (msg: string);

Food :: enum
{
    Apple,
    Banana,
    Bread,
    Cake,
}

Tag :: struct
{
    name: string;
    var: i32;
}

Player :: struct
{
    age := 0;
    tag: Tag;
}

main :: () -> i32
{
    player := Player {
        18,
        Tag {
            "Player 1",
            5,
        }
    };
    for i: 0..player.tag.var {
        printf("loop\n");
    }

    return 0;
}