printf :: (msg: string);

get_messages :: () -> [5]string {
    return [] {
        "One",
        "Two",
        "Three",
        "Four",
        "Five",
    };
}

main :: () -> i32
{
    messages := get_messages();

    for msg: messages {
        printf(msg);
        printf("\n");
    }

    return 0;
}